# Experimental controls

These modules are not original-paper baseline reproductions and are not exposed
by the strict training factory. `lora_conv.py` is retained only for historical
comparison and must not be labeled as LoRA in result tables.
